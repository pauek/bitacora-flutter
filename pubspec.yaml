name: bitacora
description: A new Flutter project.
version: 1.0.0+1

environment:
  sdk: ">=2.3.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.2
  hive: ^1.1.1
  hive_flutter: ^0.2.1
  path_provider: ^1.4.2

dev_dependencies:
  hive_generator: ^0.5.2
  build_runner: any
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
